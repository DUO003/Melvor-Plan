extends Button
var 测试
var 功能列表 = []
var 功能字典 = {
	"通用": ["测试金币","测试图纸","测试打印数组","测试红点","存档红点"],
	"合成界面": ["手工熟练度","手工精通","手工资源获取","测试奖励容器"],
	"背包界面": ["获取装备", "刷新商店"],
	"战斗界面": ["副本-生成敌人","高效探索"],
	"小游戏界面": [],
	"任务窗口": ["删除并退出"],
	"订单界面": ["随机资源订单"]}
var 空节点
var 下拉菜单
func _ready():
	# Godot 4.x 信号连接语法
	if OS.has_feature("editor_runtime"):
		功能字典["任务窗口"].insert(0, "直接解锁")
	空节点=$"../../.."
	gui_input.connect(func(按键信号): # 确保节点可以接收鼠标事件
		if 按键信号 is InputEventMouseButton and 按键信号.pressed:
			测试按钮(按键信号))
	下拉菜单 = $测试功能
	更新功能()
	下拉菜单.item_selected.connect(func(_序号):print("测试功能选择:",下拉菜单.text))
	空节点.场景更新.connect(更新功能)
	
func 测试按钮(按键信号):
	var 文本=$测试功能.text
	初始化.梅声音单例.播放鼠标点击音效()
	print("当前选中的测试功能：", 文本)
	if 文本=="获取装备":
		var 随机装备数组=["标准剑","标准盾","标准头盔","标准重甲","标准腿甲","标准鞋子","青铜剑","铁剑"]
		var 随机索引 = randi() % 随机装备数组.size()
		初始化.获得物品语法糖(随机装备数组[随机索引],1,"装备物品")
	elif 文本=="刷新商店":
		初始化.商店刷新()
	elif 文本=="手工熟练度":
		if not 初始化.梅存档["手工"].has("熟练"):
			初始化.梅存档["手工"]["熟练"]=0
		初始化.梅存档["手工"]["熟练"]+=1000
		初始化.结算升级("手工")
		初始化.emit_signal("更新_UI")
	elif 文本=="手工精通":
		if not 初始化.梅存档["手工"].has("精通"):
			初始化.梅存档["手工"]["精通"]=0
		初始化.梅存档["手工"]["精通"]+=1000
		初始化.结算升级("手工")
		初始化.emit_signal("更新_UI")
	elif 文本=="测试金币":
		初始化.梅存档["金币"]+=10000
		初始化.emit_signal("更新_UI")
	elif 文本=="测试图纸":
		初始化.获得物品语法糖("蓝图纸",1600)
	elif 文本=="手工资源获取":
		初始化.获得资源("木材", 1000, true, false)
		初始化.获得资源("矿石", 1000, true, false)
		初始化.获得资源("皮革", 1000, true, false)
		初始化.获得资源("药草", 1000, true, false)
		初始化.emit_signal("更新_UI")
	elif 文本=="副本-生成敌人":
		if 初始化.节点.has("战斗副本节点"):
			if is_instance_valid(初始化.节点["战斗副本节点"]):
				初始化.节点["战斗副本节点"].生成敌人()
	elif 文本=="高效探索":
		if 节点有效性检查("战斗副本节点"):
			if is_instance_valid(初始化.节点["战斗副本节点"]):
				if 初始化.节点["战斗副本节点"].副本数据["图块探索效率"]==初始化.节点["战斗副本节点"].图块探索效率:
					初始化.节点["战斗副本节点"].图块探索效率=25
					初始化.节点["战斗副本节点"].图块探索速度=0.1
				else :
					初始化.节点["战斗副本节点"].图块探索效率=初始化.节点["战斗副本节点"].副本数据["图块探索效率"]
					初始化.节点["战斗副本节点"].图块探索速度=初始化.节点["战斗副本节点"].副本数据["图块探索速度"]
				初始化.节点["战斗副本节点"].更新副本显示信息()
	elif 文本=="测试打印数组":
		var 数组=[1,1,3,1,1]
		seed(123)
		数组.shuffle()
		print(数组)
		数组=[1,2,3,4,5]
		seed(123)
		数组.shuffle()
		print(数组)
		return
	elif 文本=="测试红点":
		if 按键信号.button_index == MOUSE_BUTTON_RIGHT:
			初始化.梅红点单例.消除红点("测试红点","",-1)
		else:
			初始化.梅红点单例.红点存档+=["测试红点"]
			初始化.梅红点单例.加载红点存档()
			初始化.emit_signal("更新红点")
		return
	elif 文本=="存档红点":
		if 节点有效性检查("空节点"):
			var 当前界面名称=初始化.节点["空节点"].初始界面
			初始化.梅红点单例.红点存档+=[当前界面名称]
			初始化.梅红点单例.加载红点存档()
			初始化.emit_signal("更新红点")
		return
	elif 文本=="直接解锁":
		var 缓存界面=初始化.梅窗口单例.界面父子关系
		var 窗口解锁 = 初始化.梅存档["挂机"].get("窗口解锁",[])
		for 窗口名称 in 缓存界面:
			if not 窗口解锁.has(窗口名称):
				窗口解锁.append(窗口名称)
				初始化.梅存档["挂机"]["窗口解锁"] = 窗口解锁
		初始化.解锁系统("手工")
		if 节点有效性检查("空节点"):
			初始化.节点["空节点"].生成任务栏按钮()
		return
	elif 文本=="随机资源订单":
		if 节点有效性检查("订单界面"):
			初始化.节点["订单界面"].新增订单(1)
			初始化.节点["订单界面"].加载订单()
		初始化.保存存档("测试资源订单")
		return
	elif 文本=="删除并退出":
		初始化.删除存档()
		return
	elif 文本=="测试奖励容器":
		if 节点有效性检查("奖励悬浮面板"):
			var 物品数组=[]
			var 物品类型数组=获取配方("炼金材料")+获取配方("催化剂")+["金币"]
			print(物品类型数组)
			if 按键信号.button_index == MOUSE_BUTTON_RIGHT:
				for X in 10:
					物品数组+=[初始化.语法糖获得物品(物品类型数组[randi() %物品类型数组.size()],10+randi() %20)]
			else :
				物品数组+=[初始化.语法糖获得物品(物品类型数组[randi() %物品类型数组.size()],10+randi() %20)]
			初始化.节点["奖励悬浮面板"].物品数组+=物品数组
		return
	else :
		return
	初始化.保存存档("点击测试按钮")
	#GBIS.inventory_service.消耗指定数量物品("背包","蓝图纸",10)
	#初始化.零点检测()
	#初始化.梅存档["手工"]["熟练"]+=1000
	#初始化.时间戳字典["手工熟练"]+=-300
	#print("当前场景根节点名称: ", 初始化.节点["合成界面"].name)
	
	#初始化.调试精通()
	#初始化.获得物品语法糖("标准剑",5,"标准物品")
	#调试物品字典()
	#测试=初始化.初始化装备("标准盾")
	#print("测试",测试)
	#测试.属性测试()
	#测试=初始化.初始化装备("铁剑")
	#print("测试",测试)
	#测试.属性测试()
	#
	#初始化.玩家单例.打印属性()
	#var 剩余秒数 = int(初始化.商店刷新计时器.time_left)
	#var 格式化时间文本 = 初始化.格式化时间(剩余秒数)
	#print("商店刷新剩余时间：", 格式化时间文本)
	#初始化.emit_signal("更新_UI")# 发送UI更新信号

func 更新功能(场景名称=null):
	print("更新列表")
	功能列表=[]
	if not 场景名称==null and 功能字典.has(场景名称):
		功能列表+=功能字典[场景名称]
	功能列表+=功能字典["通用"]
	下拉菜单.clear()
	for 功能名称 in 功能列表:
		下拉菜单.add_item(功能名称)
	pass

func 打印字典内容(字典:Dictionary)-> void:
	for 键 in 字典:
		print ("键:", 键,"，值:", 字典 [键])
func 节点有效性检查(节点名称:String)->bool:
	return 节点名称 in 初始化.节点 and 初始化.节点[节点名称] != null

func 调试物品字典() -> void:
	# 打印字典基本信息（使用repeat方法替代*运算符）
	print("=".repeat(60))
	print("初始化.物品字典 调试信息 - 共 %d 个物品" % 初始化.物品字典.size())
	print("=".repeat(60))
	
	# 遍历物品字典
	for 物品键 in 初始化.物品字典:
		var 物品数据 = 初始化.物品字典[物品键]
		print("\n【物品键】: %s" % 物品键)
		print("【物品名称】: %s" % 物品数据.item_name)
		print("-".repeat(50))
		
		# 获取并打印物品的所有属性
		var 属性列表 = 物品数据.get_property_list()
		for 属性 in 属性列表:
			var 属性名 = 属性.name
			# 过滤内部属性和信号
			if 属性名.begins_with("_") or 属性.type == TYPE_SIGNAL:
				continue
			
			# 打印属性名和值
			var 属性值 = 物品数据.get(属性名)
			print("%-20s: %s" % [属性名, 属性值])
	
	# 调试结束标记
	print("\n" + "=".repeat(60))
	print("初始化.物品字典 调试完成")
	print("=".repeat(60))
func 获取配方(类型参数="基础素材"):
	var 图纸数据=梅表格.装备蓝图
	var 图纸条件=图纸数据[0].find("类型")
	if 类型参数=="特殊":
		图纸条件=图纸数据[0].find("图纸集")
	var 图纸数组=[]
	for 图纸 in 图纸数据:
		if 图纸[图纸条件] ==类型参数:
			图纸数组+=[图纸[0]]
	return 图纸数组
